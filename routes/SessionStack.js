/*import {createStackNavigator} from '@react-navigation/stack';
import {createAppContainer} from 'react-navigation'
import sessions from '../pages/sessionsMenu';
import menu from '../pages/breakfast';

const screens= {
    sessions: {
        screen: sessions
    },
    menu: {
        screen: menu
    }
}

const SessionStack = createStackNavigator(screens);

export default createAppContainer(sessionStack);*/